{
  "info": "SQL Quick Reference",
  "version": "1.0",

  "basics": {
    "SELECT": "SELECT * FROM table",
    "WHERE": "SELECT * FROM table WHERE column = 'value'",
    "INSERT": "INSERT INTO table (col1, col2) VALUES ('a', 'b')",
    "UPDATE": "UPDATE table SET col1 = 'new' WHERE id = 1",
    "DELETE": "DELETE FROM table WHERE id = 1"
  },

  "filtering": {
    "AND": "WHERE a = 1 AND b = 2",
    "OR": "WHERE a = 1 OR b = 2",
    "NOT": "WHERE NOT a = 1",
    "IN": "WHERE id IN (1, 2, 3)",
    "BETWEEN": "WHERE age BETWEEN 18 AND 30",
    "LIKE": "WHERE name LIKE '%john%'"
  },

  "aggregates": {
    "COUNT": "SELECT COUNT(*) FROM table",
    "SUM": "SELECT SUM(amount) FROM table",
    "AVG": "SELECT AVG(price) FROM table",
    "MIN/MAX": "SELECT MIN(age), MAX(age) FROM table",
    "GROUP_BY": "SELECT status, COUNT(*) FROM table GROUP BY status",
    "HAVING": "GROUP BY ... HAVING COUNT(*) > 1"
  },

  "sorting": {
    "ASC": "ORDER BY name ASC",
    "DESC": "ORDER BY date DESC",
    "MULTIPLE": "ORDER BY name ASC, date DESC"
  },

  "joins": {
    "INNER": "SELECT * FROM a INNER JOIN b ON a.id = b.a_id",
    "LEFT": "SELECT * FROM a LEFT JOIN b ON a.id = b.a_id",
    "RIGHT": "SELECT * FROM a RIGHT JOIN b ON a.id = b.a_id",
    "CROSS": "SELECT * FROM a CROSS JOIN b"
  },

  "common": {
    "LIMIT": "LIMIT 10",
    "OFFSET": "LIMIT 10 OFFSET 20",
    "DISTINCT": "SELECT DISTINCT column FROM table",
    "NULL_CHECK": "WHERE column IS NULL / IS NOT NULL",
    "CASE": "SELECT CASE WHEN age > 18 THEN 'Adult' ELSE 'Minor' END FROM table"
  }
}
